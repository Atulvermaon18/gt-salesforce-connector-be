// const express = require("express");
// const { authorizePermission, protect } = require('../middlewares/authHandler.js');

// const router = express.Router();

// router.get("/", protect, authorizePermission('SETTINGS'),roleController.getRoles);
// router.post("/", protect, authorizePermission('SETTINGS'),roleController.createRole);


// module.exports = router;
